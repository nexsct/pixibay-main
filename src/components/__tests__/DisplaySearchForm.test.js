const assert = require('assert');
const DisplaySearchForm = require('../DisplaySearchForm.js');

describe('DisplaySearchForm', () => {
  it('has a test', () => {
    assert(false, 'DisplaySearchForm should have a test');
  });
});
