const assert = require('assert');
const DisplaySearchResults = require('../DisplaySearchResults.js');

describe('DisplaySearchResults', () => {
  it('has a test', () => {
    assert(false, 'DisplaySearchResults should have a test');
  });
});
